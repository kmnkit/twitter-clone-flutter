import 'package:flutter/foundation.dart';

// class ThreadConfig extends ChangeNotifier {
//   bool autoMute = true;
// }

// final threadConfig = ThreadConfig();

final threadConfig = ValueNotifier(false);
